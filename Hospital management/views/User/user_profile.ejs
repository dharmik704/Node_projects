<%- include('user_header') %>

            <!-- Table Start -->
            <center>
                <div class="container-fluid pt-4 px-4">
                    <!-- <img src="<%= locals.doctor.doctorimg %>" alt="image" width="150" style="border-radius: 50%; margin-bottom: 20px;"> -->
                    
                    <div class="row g-3" style="justify-content: center; margin: 80px 0;">
                        <a style="color: green" href="/user/updateprofile/<%= locals.user.id %>">Edit <i class="fa fa-edit"></i></a>
                        <div class="col-sm-12 col-xl-6">
                            <div class="bg-light rounded h-100 p-4">
                                <h6 class="mb-4">Your Profile</h6>
                                <table class="table table-hover">
                                    <tbody>
                                        <tr>
                                            <th scope="row">User Name</th>
                                            <td><%= locals.user.name %></td>
                                        </tr>
                                        <tr>
                                            <th scope="row">Email</th>
                                            <td><%= locals.user.email %></td>
                                        </tr>
                                        <tr>
                                            <th scope="row">Role</th>
                                            <td><%= locals.user.role %></td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </center>
            <!-- Table End -->

<%- include('user_footer') %>